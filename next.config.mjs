// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If your app uses experimental features, add them here.
  // experimental: { serverActions: true },
};

export default nextConfig;
